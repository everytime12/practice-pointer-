#include<stdio.h>
#include<string.h>
#include<stdlib.h>
struct{
    char name[20];
    int age;
    float salary;
    char hobby[3][20];
} employee;
int main(){
    employee.age=1;
    printf("%d",employee.age);
    strcpy(empolyee.name, "Neil Diamond");
    strcpy(employee.hobby[2]," tennis and walking");
}
